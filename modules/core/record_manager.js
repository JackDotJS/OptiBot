const memory = require(`./memory.js`);
const RecordEntry = require(`./record_entry.js`);

module.exports = class OptiBotRecordManager {
  constructor() {
    throw new Error(`Why are you doing this? (Cannot instantiate this class.)`);
  }

  static get() {
    // todo
  }
}